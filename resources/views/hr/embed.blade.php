<x-app-layout>
    <div class="h-[calc(100vh-4rem)] flex flex-col">
        <iframe
            src="{{ $iframeSrc }}"
            class="flex-1 w-full border-0"
            allow="fullscreen"
        ></iframe>
    </div>
</x-app-layout>
