HR migrations translated from the standalone kashtre-hr app should live here.

Do not copy the standalone migration set into this directory unchanged.
That repo creates duplicate base tables such as users and password reset tables,
and it models organizations separately from the main app businesses table.
